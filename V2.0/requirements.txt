# 4DOF Scanner Control System - Python Dependencies
# Compatible with Python 3.10+

# Core dependencies
numpy>=1.21.0
pyserial>=3.5
pyserial-asyncio>=0.6
pyyaml>=6.0

# Async support
asyncio-mqtt>=0.13.0

# Camera and image processing
picamera2>=0.3.0
opencv-python>=4.5.0
pillow>=8.0.0

# Web interface
flask>=2.0.0
flask-cors>=3.0.0
# gunicorn removed - Flask is more reliable for Pi hardware

# GPIO control for Pi5
gpiozero>=1.6.0
pigpio>=1.78  # For precise PWM timing
rpi-hardware-pwm>=0.2.0  # Direct hardware PWM access via dtoverlay

# Development and testing
pytest>=7.0.0
pytest-asyncio>=0.21.0

# Data handling
requests>=2.25.0
psutil>=5.8.0  # System monitoring

# Development and testing
pytest>=6.0.0
pytest-asyncio>=0.18.0

# Optional: Enhanced functionality
matplotlib>=3.5.0  # For visualization/debugging
scipy>=1.7.0       # For advanced path planning algorithms

# Note: Some packages may require system-level installation on Pi5:
# sudo apt update
# sudo apt install python3-picamera2 python3-opencv python3-gpiozero
# sudo systemctl enable pigpiod
# sudo systemctl start pigpiod